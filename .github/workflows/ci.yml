name: CI Pipeline V2

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build
        run: echo "Build Successful V2 🚀"

      - name: Slack Notification
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"CI/CD Build Successful 🚀"}' \
          ${{ secrets.SLACK_WEBHOOK_V2 }}